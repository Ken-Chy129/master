<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ken.master.server.mapper.FieldMapper">

    <select id="count" resultType="java.lang.Long">
        select count(*) from main.master_field
        <where>
            app_id = #{appId}
            <if test="namespaceId != null">
                and namespaceId = #{namespaceId}
            </if>
            <if test="name != null">
                and name like #{name}%
            </if>
        </where>
    </select>


    <select id="selectByCondition" resultType="cn.ken.master.server.model.entity.FieldDO">
        select * from main.master_field
        <where>
            app_id = #{appId}
            <if test="namespaceId != null">
                and namespaceId = #{namespaceId}
            </if>
            <if test="name != null">
                and name like #{name}%
            </if>
        </where>
        limit #{startIndex}, #{pageSize}
    </select>
</mapper>
